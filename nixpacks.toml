# Simple nixpacks.toml for Chatwoot
[variables]
NODE_VERSION = "18"
RUBY_VERSION = "3.2.2"

[phases.build]
cmds = ["bundle exec rake assets:precompile"]

[start]
cmd = "bundle exec rails server -b 0.0.0.0 -p $PORT"
